import { ButtonDebounceDirective } from './button-debounce.directive';

describe('ButtonDebounceDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonDebounceDirective();
    expect(directive).toBeTruthy();
  });
});
