import { Component, Input } from '@angular/core';

@Component({ selector: 'tripsultant-apps-map-transit-directions-information', template: '' })
export class TransitDirectionsInformationStubComponent {
  @Input() routeIndex: number;
}
