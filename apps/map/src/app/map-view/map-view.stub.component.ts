import { Component, Directive, Input } from '@angular/core';

@Component({ selector: 'tripsultant-apps-map-map-view', template: '' })
export class MapViewStubComponent {
  @Input() tripIndex: number;
}
