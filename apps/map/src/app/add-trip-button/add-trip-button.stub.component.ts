import { Component, Input } from '@angular/core';

@Component({ selector: 'tripsultant-apps-map-add-trip-button', template: '' })
export class AddTripButtonStubComponent {
  @Input() displayType: string;
}
