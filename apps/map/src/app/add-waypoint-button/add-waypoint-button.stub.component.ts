import { Component, Input } from '@angular/core';

@Component({ selector: 'tripsultant-apps-map-add-waypoint-button', template: '' })
export class AddWaypointButtonStubComponent {
  @Input() displayType: string;
}
